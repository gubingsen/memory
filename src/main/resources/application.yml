#设置服务器的端口号
server:
  port: 8080
#数据库连接设置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/memory?userSSL=false&characterEncoding=utf-8&serverTimezone=GMT
    username: root
    password: 878166
    type: com.alibaba.druid.pool.DruidDataSource
#mybatis的配置
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.three.memory.domain
  #配置sql语句的输出到控制台
logging:
  level:
    com:
      three:
        memory:
          mapper: debug